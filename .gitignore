# 编译目录
/target

# 测试目录
.test/

# 本助手的项目记忆目录
.workbuddy/

# 运行时产物
*.log
*.pid
*.db
*.db-wal
*.db-shm

# 上传/媒体产物
uploads/

# 本地分析产物（cargo metadata 等手动导出的快照）
cargo_meta.json

# 文档：PDF 等二进制不纳入版本控制
*.pdf

# 本地issue文档
docs/issues

# 参考项目代码
.ref/

# 敏感凭据（.env 由 llaia init 生成，config.toml 用 ${VAR} 引用）
.env
.env.*
